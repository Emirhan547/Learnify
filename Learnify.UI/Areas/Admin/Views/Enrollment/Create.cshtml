@model Learnify.DTO.DTOs.EnrollmentDto.CreateEnrollmentDto
@{
    ViewData["Title"] = "Yeni Kayıt Ekle";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container mt-4">
    <div class="card shadow-sm border-0 rounded-3">
        <div class="card-header bg-primary text-white fw-semibold">
            Yeni Kayıt Ekle
        </div>

        <div class="card-body">
            <form asp-action="Create" method="post">
                <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="StudentId" class="form-label fw-semibold">Öğrenci</label>
                        <select asp-for="StudentId"
                                asp-items="ViewBag.Students ?? new List<SelectListItem>()"
                                class="form-select">
                            <option value="">Öğrenci Seçiniz</option>
                        </select>
                        <span asp-validation-for="StudentId" class="text-danger"></span>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label asp-for="CourseId" class="form-label fw-semibold">Kurs</label>
                        <select asp-for="CourseId"
                                asp-items="ViewBag.Courses ?? new List<SelectListItem>()"
                                class="form-select">
                            <option value="">Kurs Seçiniz</option>
                        </select>
                        <span asp-validation-for="CourseId" class="text-danger"></span>
                    </div>
                </div>

                <div class="mb-3">
                    <label asp-for="EnrolledDate"class="form-label fw-semibold">Kayıt Tarihi</label>
                    <input asp-for="EnrolledDate" class="form-control" type="date" />
                    <span asp-validation-for="EnrolledDate" class="text-danger"></span>
                </div> 

                <div class="d-flex justify-content-end mt-4">
                    <button type="submit" class="btn btn-success me-2">
                        <i class="bi bi-save"></i> Kaydet
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Geri Dön
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>
