@using Learnify.DTO.DTOs.LessonDto
@model UpdateLessonDto
@{
    ViewData["Title"] = "Ders Güncelle";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="right_content">
    <div class="container-fluid mt-4">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="white_shd full p-4 shadow-sm rounded-3">
                    <div class="heading1 mb-4">
                        <h2 class="fw-bold">Ders Güncelle</h2>
                    </div>

                    <form asp-action="UpdateLesson" method="post">
                        <input type="hidden" asp-for="LessonID" />

                        <div class="mb-3">
                            <label class="form-label fw-bold">Ders Başlığı</label>
                            <input asp-for="Title" class="form-control" placeholder="Örn: Giriş ve Temel Kavramlar" />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Video URL</label>
                            <input asp-for="VideoUrl" class="form-control" placeholder="https://www.youtube.com/watch?v=..." />
                            <span asp-validation-for="VideoUrl" class="text-danger"></span>
                            <small class="form-text text-muted">YouTube, Vimeo veya diğer video platformlarından link ekleyin</small>
                        </div>

                        <!-- ✅ Kurs seçimi eklendi -->
                        <div class="mb-3">
                            <label class="form-label fw-bold">Kurs Seçin</label>
                            <select asp-for="CourseID" class="form-control">
                                <option value="">Kurs Seçiniz</option>
                                @foreach (var course in ViewBag.Courses)
                                {
                                    <option value="@course.CourseID">@course.Title</option>
                                }
                            </select>
                            <span asp-validation-for="CourseID" class="text-danger"></span>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="fa fa-save"></i> Güncelle
                            </button>
                            <a href="/Admin/Lesson/Index" class="btn btn-secondary">
                                <i class="fa fa-times"></i> İptal
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}