@model Learnify.DTO.DTOs.NotificationDto.ResultNotificationDto
@{
    Layout = "~/Views/Shared/_StudentPanelLayout.cshtml";
}

<h3 class="fw-bold mb-3">@Model.Title</h3>
<p class="text-muted">@Model.CreatedDate.ToString("dd MMM yyyy HH:mm")</p>

<div class="alert alert-secondary p-3">
    @Model.Content
</div>

<a asp-action="Index" class="btn btn-primary mt-3">← Bildirimlere Dön</a>
